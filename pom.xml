<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright 2025 - 2026 Dmitri Snytkine. All rights reserved.

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

        Unless required by applicable law or agreed to in writing, software
        distributed under the License is distributed on an "AS IS" BASIS,
        WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.

        See the License for the specific language governing permissions and
        limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.1</version>
		<relativePath /> <!-- lookup parent from repository -->
	</parent>
	<groupId>io.github.snytkine</groupId>
	<artifactId>wiremock-interceptor-spring-boot-starter</artifactId>
	<packaging>jar</packaging>
	<version>0.0.5-SNAPSHOT</version>
	<!-- Fix #6: align <name> with <artifactId> -->
	<name>wiremock-interceptor-spring-boot-starter</name>
	<description>Spring Boot Starter for WireMock RestClient Interceptor</description>
	<url>https://github.com/snytkine/rest-client-wiremock-middleware</url>
	<inceptionYear>2025</inceptionYear>
	<developers>
		<developer>
			<id>snytkine</id>
			<name>Dmitri Snytkine</name>
			<email>d.snytkine@gmail.com</email>
			<url>https://github.com/snytkine</url>
		</developer>
	</developers>
	<licenses>
		<license>
			<name>Apache 2</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>
	<scm>
		<connection>scm:git:https://github.com/snytkine/rest-client-wiremock-middleware.git</connection>
		<developerConnection>scm:git:https://github.com/snytkine/rest-client-wiremock-middleware.git</developerConnection>
		<tag>HEAD</tag>
		<url>https://github.com/snytkine/rest-client-wiremock-middleware</url>
	</scm>
	<issueManagement>
		<system>GitHub</system>
		<url>https://github.com/snytkine/rest-client-wiremock-middleware/issues</url>
	</issueManagement>
	<properties>
		<java.version>21</java.version>
		<lombok.version>1.18.30</lombok.version>
		<wiremock.version>3.6.0</wiremock.version>
		<wiremock.max-version>3.10.0</wiremock.max-version>
		<!-- JaCoCo configuration -->
		<jacoco.version>0.8.12</jacoco.version>
		<jacoco.minimum.instruction.coverage>0.80</jacoco.minimum.instruction.coverage>
		<jacoco.minimum.branch.coverage>0.70</jacoco.minimum.branch.coverage>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.wiremock</groupId>
			<artifactId>wiremock</artifactId>
			<version>[${wiremock.version}, ${wiremock.max-version}]</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.wiremock.extensions</groupId>
			<artifactId>wiremock-faker-extension</artifactId>
			<version>0.2.0</version>
		</dependency>

		<!-- Fix #1: mark optional so Lombok does not leak to consumers at runtime -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- Fix #3: removed explicit version; Spring Boot BOM manages mockito -->
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Spotless code formatting plugin (enforces formatting compatible with Spring
			guidelines) -->
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>3.1.0</version>
				<configuration>
					<java>
						<licenseHeader>
							<file>${project.basedir}/src/main/resources/license-header.txt</file>
						</licenseHeader>
						<googleJavaFormat>
							<version>1.17.0</version>
						</googleJavaFormat>
						<importOrder />
						<removeUnusedImports />
						<formatAnnotations />
					</java>
				</configuration>
				<executions>
					<execution>
						<id>spotless-apply</id>
						<goals>
							<goal>apply</goal>
						</goals>
						<phase>initialize</phase>
					</execution>
					<execution>
						<id>spotless-check</id>
						<goals>
							<goal>check</goal>
						</goals>
						<phase>validate</phase>
					</execution>
				</executions>
			</plugin>

			<!-- Configure annotation processors so Lombok and Spring config processor run during
			compile -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<fork>true</fork>
					<release>${java.version}</release>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<!-- Fix #5: use ${project.parent.version} instead of hardcoded 3.3.1 -->
						<path>
							<groupId>org.springframework.boot</groupId>
							<artifactId>spring-boot-configuration-processor</artifactId>
							<version>${project.parent.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!-- JaCoCo code coverage plugin - generates report and enforces minimum coverage -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>INSTRUCTION</counter>
											<value>COVEREDRATIO</value>
											<minimum>${jacoco.minimum.instruction.coverage}</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>${jacoco.minimum.branch.coverage}</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Pre-commit Maven Plugin -->
			<plugin>
				<groupId>com.rudikershaw.gitbuildhook</groupId>
				<artifactId>git-build-hook-maven-plugin</artifactId>
				<version>3.6.0</version>
				<executions>
					<execution>
						<goals>
							<goal>configure</goal>
							<goal>install</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<gitConfig>
						<core.hooksPath>src/main/resources/hooks/</core.hooksPath>
					</gitConfig>
					<hooks>
						<hook>
							<name>pre-commit</name>
							<script>src/main/resources/hooks/pre-commit</script>
						</hook>
					</hooks>
				</configuration>
			</plugin>

			<!-- Fix #4: switched from deprecated pl.project13.maven:git-commit-id-plugin
			     to the maintained io.github.git-commit-id:git-commit-id-maven-plugin,
			     which is also managed by Spring Boot's parent POM -->
			<plugin>
				<groupId>io.github.git-commit-id</groupId>
				<artifactId>git-commit-id-maven-plugin</artifactId>
				<configuration>
					<generateGitPropertiesFile>true</generateGitPropertiesFile>
					<generateGitPropertiesFilename>${project.build.outputDirectory}/git.properties</generateGitPropertiesFilename>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>0.7.0</version>
				<extensions>true</extensions>
				<configuration>
					<publishingServerId>central</publishingServerId>
					<autoPublish>true</autoPublish>
					<waitUntil>published</waitUntil>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<configuration>
					<autoVersionSubmodules>true</autoVersionSubmodules>
					<useReleaseProfile>false</useReleaseProfile>
					<releaseProfiles>release</releaseProfiles>
					<goals>spotless:apply deploy</goals>
					<preparationGoals>clean</preparationGoals>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Fix #2: GPG signing, sources JAR, and javadoc JAR are only needed when
		     publishing to Maven Central. Keeping them here prevents every `mvn verify`
		     from requiring a GPG key. Activated automatically by maven-release-plugin
		     via releaseProfiles=release, or manually with -Prelease. -->
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-source-plugin</artifactId>
						<version>3.3.1</version>
						<executions>
							<execution>
								<id>attach-sources</id>
								<goals>
									<goal>jar-no-fork</goal>
								</goals>
							</execution>
						</executions>
					</plugin>

					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<configuration>
							<doclint>none</doclint>
						</configuration>
						<executions>
							<execution>
								<id>attach-javadocs</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
					</plugin>

					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>